# ============================================
# GitHub 연동을 위한 환경 변수
# ============================================
# 이 파일을 .env로 복사한 후 실제 값으로 변경하세요:
# cp .env.github .env

# ============================================
# 필수 항목
# ============================================

# OpenAI API Key (필수!)
# https://platform.openai.com/api-keys 에서 발급
OPENAI_API_KEY=sk-your-openai-api-key-here

# GitHub Personal Access Token (필수!)
# https://github.com/settings/tokens 에서 발급
# 필요한 권한: repo, write:discussion
GITHUB_TOKEN=ghp_your-github-token-here

# ============================================
# 선택 항목
# ============================================

# OpenAI 모델 선택 (기본: gpt-4-turbo-preview)
# 비용 절감을 위해 gpt-3.5-turbo 사용 가능
OPENAI_MODEL=gpt-4-turbo-preview

# GitHub Webhook Secret (선택)
# 로컬 테스트 시 비워두거나 test-secret 사용
GITHUB_WEBHOOK_SECRET=

# ============================================
# 참고: 환경 변수 적용 방법
# ============================================
#
# 방법 1: .env 파일 사용 (권장)
#   1. 이 파일을 .env로 복사
#      cp .env.github .env
#   2. .env 파일에서 실제 값 입력
#   3. 스크립트 실행
#      ./run-with-github.sh
#
# 방법 2: 직접 export
#   export OPENAI_API_KEY=sk-xxx
#   export GITHUB_TOKEN=ghp_xxx
#   ./run-with-github.sh
#
# 방법 3: 인라인으로 실행
#   OPENAI_API_KEY=sk-xxx GITHUB_TOKEN=ghp_xxx ./run-with-github.sh
#
